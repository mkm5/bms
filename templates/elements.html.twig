{%- macro _input(classes='') -%}
    {%- set base = 'aria-disabled:cursor-not-allowed outline-none focus:outline-none text-stone-800 placeholder:text-stone-600/60 ring-transparent border border-stone-200 transition-all ease-in disabled:opacity-50 disabled:pointer-events-none select-none text-sm py-2 px-2.5 ring bg-white rounded-lg duration-100 hover:border-stone-300 hover:ring-none focus:border-stone-400 focus:ring-none peer' -%}
    {{ (base ~ ' ' ~ classes)|tailwind_merge }}
{%- endmacro _input -%}

{%- macro _checkbox(classes='') -%}
    {%- set base = 'peer h-5 w-5 cursor-pointer transition-all rounded hover:shadow border border-stone-200 checked:text-stone-800 checked:border-stone-800' -%}
    {{ (base ~ ' ' ~ classes)|tailwind_merge }}
{%- endmacro _checkbox -%}

{%- macro _label(classes='') -%}
    {%- set base = 'font-sans antialiased text-sm text-stone-800 font-semibold required select-none' -%}
    {{ (base ~ ' ' ~ classes)|tailwind_merge }}
{%- endmacro _label -%}

{%- macro _checkbox_container_block(classes='') -%}
    {%- set base = 'flex items-center gap-2' -%}
    {{ (base ~ ' ' ~ classes)|tailwind_merge }}
{%- endmacro _checkbox_container_block -%}

{%- macro _checkbox_container(classes='') -%}
    {%- set base = 'inline-flex items-center gap-2' -%}
    {{ (base ~ ' ' ~ classes)|tailwind_merge }}
{%- endmacro _checkbox_container -%}

{%- macro _table_head(classes='') -%}
    {%- set base = 'px-6 py-3 font-medium whitespace-nowrap' -%}
    {{ (base ~ ' ' ~ classes)|tailwind_merge }}
{%- endmacro -%}

{%- macro _table_column(classes='') -%}
    {%- set base = 'px-6 py-4 text-gray-500' -%}
    {{ (base ~ ' ' ~ classes)|tailwind_merge }}
{%- endmacro -%}
