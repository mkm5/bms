flysystem:
    storages:
        default.storage:
            adapter: 'local'
            options:
                directory: '%kernel.project_dir%/var/storage/default'
        documents.storage:
            adapter: 'local'
            options:
                directory: '%kernel.project_dir%/var/storage/documents'
